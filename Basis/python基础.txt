python格式化字符
%s       字符串
%d      有符号整数(十进制)
%u      无符号整数(十进制)

%3d : 表示数字的最小输出长度是3位，少于3位的左边补空格
%-3d：表示数字的最少输出长度是3位，少于3位的右边补空格

打印99乘法表
print("\n".join([" ".join(["%d*%d=%-2d" % (i,j,i*j) for j in range(1,i+1)]) for i in range(1,10)]))  #" ".join把每一行的之间加一个空格，"\n".join上一次循环结束换行




