快照并不是直接复制文件，它只是让当前的数据状态保存起来，不被覆写。所以快照很快。接下来的文件修改均不会覆写原本数据所在的磁盘区块，而是将修改部分写入其它磁盘中。

六种类型的快照技术：
1.cow
COW快照在初始化的过程中仅仅创建用来描述源数据块位置的指针信息(元数据)，而不是完整的将源数据块拷贝过来

2.存储系统将指向该block的指针指向一个新位置，然后将新数据写到该新位置






















